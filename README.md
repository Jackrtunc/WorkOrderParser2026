## Work Order Parser

The Work Order Parser is a tool designed to automate the process of updating the UNC Music Department's work order spreadsheet.

For more details on its usage click the `?` button in the app.

### Executable

You can find the jar file the app launches from in `app/WOP2.0.jar`
